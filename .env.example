# Environment variables example for nanoparticle-simulator
# Copy to .env and fill in values

# Optional: Path to additional Cantera data files
# CANTERA_DATA=/path/to/data

# Optional: Default output directory
# OUTPUT_DIR=./output

# Optional: Number of parallel workers for simulations
# NUM_WORKERS=4
