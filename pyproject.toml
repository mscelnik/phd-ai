[tool.poetry]
name = "phd-ai"
version = "0.1.0"
description = "Reimplementation of PhD soot/CNT simulation (educational, simplified)"
authors = ["phd-ai <noreply@example.com>"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 88
